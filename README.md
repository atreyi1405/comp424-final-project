See the two PDFs "code\_specification.pdf" and "project\_specification.pdf" to get started!

To pull new changes from public rep:

`git pull public master` Creates a merge commit<br>
`git push origin master`
